[RLBot Configuration]
# Configuration for running utility scripts via RLBot.

[Match Configuration]
# No bot participants; training runs as a standalone script.
num_participants = 0

[Scripts]
# Allow the training script to be launched from the RLBot GUI.
script_config_0 = ./training/bot.cfg
